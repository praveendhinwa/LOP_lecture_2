#include <bits/stdc++.h>

using namespace std;

int main()
{
    string s;
    cin >> s;

    sort (s.begin(), s.end());

    cout << s.substr (i, len) << endl;

    reverse (s.begin(), s.end());

    return 0;
}


